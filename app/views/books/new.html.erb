<% if @book.errors.any? %>
  <h2>following errors prevented the article from </h2>
  <ul>

    <%= @book.errors.full_messages.each do |msg| %>
      <li> <%= msg %>
      </li>
    <% end %>
  </ul>
<% end %>

<h2> insert data into form </h2>

<%= render 'form' %>
